---
description: "Deep vault optimization: CRM, graphs, dashboards, compression, wikilinks"
---

The user typed `/optimize-brain`. Invoke the `optimize-brain` skill via the Skill tool, forwarding any arguments the user provided after the slash command. If `optimize-brain` is not in the available-skills list, tell the user the skill isn't installed and suggest running `bash ~/.claude/skills/ai-brain-starter/bootstrap.sh` to install it.
